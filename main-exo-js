let largueur = prompt("largeur du rectangle?");
let longueur = prompte ("longueur du rectangle?");

let rectangle = new Rectangle(longueur,largeur);

alert("la surface du rectangle de longueur" ${longueur} "et de largueur" ${largeur} "est de" ${rectangle.calculersurface})
